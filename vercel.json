{
  "buildCommand": "NODE_ENV=production NEXT_CONFIG=admin ADMIN_MODE=true npx next build",
  "framework": "nextjs",
  "env": {
    "NODE_ENV": "production",
    "NEXT_CONFIG": "admin",
    "ADMIN_MODE": "true"
  },
  "build": {
    "env": {
      "NODE_ENV": "production",
      "NEXT_CONFIG": "admin",
      "ADMIN_MODE": "true"
    }
  }
}
